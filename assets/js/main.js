/**
 * Main Entry Point
 * Initializes all modules
 */

document.addEventListener('DOMContentLoaded', () => {
    initCursor();
    initParticles('bg-canvas');
    initBall();
    initUI();

    console.log('Lemon1gram Portfolio Initialized 🍋');
});
